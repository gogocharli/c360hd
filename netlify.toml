[build]
  command = "cd functions && npm i && cd .. && npm run build"
  publish = "public"
  functions = "functions"

[dev]
  framework = "svelte"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
